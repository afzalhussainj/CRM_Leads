{
  "buildCommand": "cd CRM && python manage.py collectstatic --noinput || true && cp -r staticfiles/* ../public/ 2>/dev/null || true",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "env": {
    "PYTHONPATH": "CRM"
  }
}
