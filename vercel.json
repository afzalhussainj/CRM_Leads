{
  "buildCommand": "cd CRM && python manage.py collectstatic --noinput || true",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index": { "maxDuration": 60 }
  },
  "env": {
    "PYTHONPATH": "CRM"
  }
}
