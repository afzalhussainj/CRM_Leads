{
  "buildCommand": "pip install --break-system-packages -r requirements.txt && cd CRM && python manage.py collectstatic --noinput || true",
  "rewrites": [
    { "source": "/static/(.*)", "destination": "/CRM/staticfiles/$1" },
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "env": {
    "PYTHONPATH": "CRM"
  }
}
